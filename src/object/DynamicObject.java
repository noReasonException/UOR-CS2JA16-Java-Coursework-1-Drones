package object;

import math.vector.Vector2;
import math.vector.Vector3;

public class DynamicObject extends AbstractObject {

    public DynamicObject(Vector3 position, Vector2 direction) {
        super(position, direction);
    }

}
